from plyer import notification
import time

def notify_me(title, message):
    notification.notify(
        title=title,
        message=message,
        app_icon =None,
        timeout=10,
        )
    time.sleep(20)

notify_me("Hello Mradul", "Take a break")
